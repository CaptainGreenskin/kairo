# v0.7.1 Canary Review

## Scope

- 变更对象：ToolResultBudget + 工具执行错误分类字段。
- 目标：确认补丁不引入回归，并可作为 v0.8 输入契约。

## Evidence

- 命令：`mvn -pl kairo-core -Dtest=ToolResultBudgetTest,ReActLoopTest test`
- 结果：`BUILD SUCCESS`
- 关键测试：
  - `ToolResultBudgetTest`（2/2 通过）
  - `ReActLoopTest`（20/20 通过）

## Observed Signals

- 大工具结果被裁剪并带有预算尾注。
- TOOL 消息写入预算元数据字段。
- 工具执行失败与 hook 拒绝/取消/跳过具有结构化分类字段。
- 未发现现有 ReAct 回归。

## Decision Gate

- 是否允许进入 v0.8：`Yes`
- 条件：
  - 继续在预发灰度中采集真实裁剪率与错误码分布；
  - 若业务关键工具出现过度裁剪，增加工具级白名单或分级阈值。
