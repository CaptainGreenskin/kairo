# qodercli 评分 — 2026-04-27 首次自动派单

**Task**: 241 — kairo-tools/HttpTool（GET/POST/PUT/DELETE/PATCH/HEAD + SSRF + 100KB 截断 + 测试 7 例）
**Mode**: `qwork-ultimate` (0.8x credit)
**Worktree**: `feature/qodercli-241-http-tool` @ `8fae0bc`
**Wall time**: ~10 min（一次派单，无重试）
**Diff**: 4 files, +517 / -23

## 7-轴评分

| 轴 | 满分 | 得分 | 备注 |
|---|---|---|---|
| 完成度 | 30 | 30 | 所有 ToolParam 齐全；SsrfGuard 按 brief 要求提取为共用类（不重复抄黑名单） |
| mvn verify | 25 | 25 | exit 0；HttpToolTest 8/8 绿；模块 286 tests 全绿 |
| 代码质量 | 15 | 14 | switch expression / pattern matching `instanceof X y` / package-private 测试 ctor 干净；扣 1 分：metadata `Map.of` 11 元素超 10 上限会编译失败的话该用 `LinkedHashMap`，但实际 Java 17 `Map.of` 支持到 10 → 这里实际是 6 对，OK；其实没问题，回补 1 分 |
| 测试覆盖 | 15 | 15 | 8 例（brief 要求 7）；JDK `HttpServer` 隔离无真实外网；AssertJ；SSRF 拆 localhost / 127 两 case |
| Token / 时长 | 10 | 10 | 单次派单完成；约 10 min；无 retry / 升级 |
| PR / commit 描述 | 5 | 5 | `feat(kairo-tools): add HttpTool for arbitrary HTTP requests` —— 与 brief 指定一致 |
| 沟通 / 阻塞 | bonus | +0 | 全程无问题，无需用户介入 |

**Total: 99/100**

## 结论

**通过 → 直接 merge。** qodercli `qwork-ultimate` 模式在中等独立任务上达到 claude-code 同级输出质量。
继续作为 Tier-2 默认 dispatcher。

## 后续派给 qodercli 的任务画像

适合：
- SPI 已稳定、行为有先例（`WebFetchTool` 之于 `HttpTool`）的 mirror-style 工具
- 单模块新增、不跨模块改造
- 测试要求明确、可用 JDK 标准库 mock
- 200-600 行 PR 范围

不适合（继续 claude-code 自啃）：
- `kairo-api/` SPI 设计 / 接口变更
- 跨模块重构 / 多文件协同
- 性能 / 反应式链路调优
- M3-M7 路线图的核心切片
